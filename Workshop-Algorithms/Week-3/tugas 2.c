#include <stdio.h>

int main(void)
{
  int n, kg, ms;

  /* I'm assigned a value to variable.
   * To put it in simple terms, I put something to a random box
   * On my mom's basement, that box have stickers that I give it name
   * kg and ms */
  kg = 10;
  ms = 20;

  n = kg * ms;

  printf("Gayanya adalah %d\n", n);

  /* Program exited successfully */
  return 0;
}
