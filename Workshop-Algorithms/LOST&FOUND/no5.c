#include <stdio.h>

int main()
{
    int x,total;
    char nig[12];
for (int a=1; a<=100 ; a++) {
    if (a%2==0) {
        printf("Bil genap %d\n", a);
        x+=a;
    }

}
    printf("Total banyak bilangan genap: %d", x);
}
